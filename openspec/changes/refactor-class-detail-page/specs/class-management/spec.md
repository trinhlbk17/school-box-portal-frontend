## ADDED Requirements

### Requirement: Consolidate Teachers into Class Detail
The class detail section must render the list of assigned teachers instead of utilizing a separate top-level tab.

#### Scenario: Viewing assigned teachers
- **WHEN** the user views the Class Detail tab
- **THEN** they see the class's metadata and a sub-section listing all assigned teachers, along with actions to add or remove teachers.
